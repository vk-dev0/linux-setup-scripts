# How to use
after cloning the repo, you need to switch the branch to the one that contains the setup wanted

---

### TODO:

auto-install dependencies
minimal recommended libs flag
add tmux conf

### libs:

dos2unix -> convert dos to unix format
tmux -> better terminal sessions
chromium -> web browser
cage -> minimal ui environment
pv -> progress bar
